기존 환경 복사하여 새로운 
conda create -n [env-name] --clone [src-env-name]
출처: https://jangjy.tistory.com/341 [살다보니..]

가상환경 삭제
conda remove --name 환경이름 --all

yml 파일 이용하여 환경 설치
conda env create --file yml_file.yml

콘다 환경확인
conda info --envs

conda channel 선택
conda install numpy=(version) -c (channel)
channel: anaconda, conda-forge, ...
